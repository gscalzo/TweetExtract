# Agent Instructions

- Always run tests before replying with final changes. Use `pnpm test`.
- If tests fail or cannot be run, state that clearly and include the failure output or reason.
- When committing, review the diff and propose a concise, accurate commit message that describes the primary change.
